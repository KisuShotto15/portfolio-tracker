{
  "name": "portfolio-tracker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npm run build && node e2e/sync.e2e.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
